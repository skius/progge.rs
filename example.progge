fn analyze(x: int, y: int) -> int {
    let unknown = false;

    if x < y && unknown {
        while x < y {
            x = x + 1;
            y = y - 1;
        }
        let z = y - x;
        return z;
    }
    return x;
}

fn masdsdin(x: int, x: int) {
    let res = 0;
    if x < 100 {
        testcase!;
        if x > 100 {
            unreachable!;
        }
        res = 1000;
    }
    res = res;
    return res;

}

fn ex(i: int) -> int {

}